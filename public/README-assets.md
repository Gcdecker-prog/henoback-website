# Public assets

Drop the official brand logo here as `henoback-office-logo.png`, then update `siteConfig.logo.src` in `lib/site-config.ts`.

Until then, `henoback-office-logo.svg` is a text placeholder for local dev.
